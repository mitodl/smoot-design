import { Meta, Title, Stories } from "@storybook/blocks"
import * as RemoteTutorDrawer from "./RemoteTutorDrawer.stories"

<Meta of={RemoteTutorDrawer} />

<Title />

RemoteTutorDrawer now aliases AiDrawerManager and should be rendered once on the page except for the
case that props cannot be shared across drawer instances. The RemoteTutorDrawer is not exported externally.

<Stories includePrimary={false} />
